/* ============================================================
   tips.js — RETIRED
   The floating command-tip bubble has been replaced by the
   scrolling ticker bar (js/ticker.js).
   This file is kept as an empty stub so the App.Tips namespace
   key exists and any external references don't throw.
   ============================================================ */

'use strict';

const Tips = (() => {
  // no-op — feature retired
  return { init: () => {} };
})();

App.Tips = Tips;
