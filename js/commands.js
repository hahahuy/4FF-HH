/* ============================================================
   js/commands.js — SHIM (delete after Phase 5)
   Real commands are in js/commands/.
   This file exists only for backward-compatibility during transition.
   ============================================================ */
// Commands is set on window by js/commands/index.js
